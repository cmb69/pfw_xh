<?xml version="1.0" encoding="UTF-8"?>

<project name="Pfw" default="help">

    <fileset id="php-sources" dir=".">
        <include name="index.php"/>
        <include name="classes/*.php"/>
        <include name="classes/**/*.php"/>
    </fileset>

    <fileset id="tests" dir="tests">
        <include name="*Test.php"/>
    </fileset>

    <target name="help" description="lists available targets">
        <exec command="phing -l" outputProperty="help"/>
        <echo>${help}</echo>
    </target>

    <target name="sniff" description="checks adherence to coding standards">
        <phpcodesniffer standard="PSR2">
            <fileset refid="php-sources"/>
            <fileset refid="tests"/>
        </phpcodesniffer>
    </target>

    <target name="mess" description="detects code flaws">
        <phpmd>
            <fileset refid="php-sources"/>
            <fileset refid="tests"/>
         </phpmd>
    </target>

    <target name="compat" description="checks PHP requirements">
        <exec command="phpcompatinfo analyser:run --alias current" logoutput="true"/>
    </target>

    <target name="test" description="runs all tests">
        <phpunit haltonerror="true" haltonfailure="true"
                bootstrap="tests/bootstrap.php">
            <formatter type="plain" usefile="false"/>
            <batchtest>
                <fileset refid="tests"/>
            </batchtest>
        </phpunit>
    </target>

    <target name="coverage" description="generates code coverage report">
        <exec command="phpunit --configuration coverage.xml" logoutput="true"/>
    </target>

    <target name="markup" description="generates HTML help">
        <exec command="pandoc help/help.md -t html5 --toc -s -o help/help.htm"/>
    </target>

    <target name="doc" description="generates API documentation">
        <mkdir dir="docs/api"/>
        <phpdoc2 title="Pfw_XH" destdir="docs/api" defaultPackageName="Pfw"
                 pharlocation="phpDocumentor.phar" template="clean">
            <fileset refid="php-sources"/>
        </phpdoc2>
    </target>

    <target name="build" description="builds a distributable ZIP archive">
        <fail unless="version" message="version is not defined!"/>
        <exec command="git archive -o export.tar HEAD" checkreturn="true"/>
        <untar file="export.tar" todir="export"/>
        <delete file="export.tar"/>
        <move todir="dist">
            <fileset dir="export">
                <include name="index.php"/>
                <include name="version.nfo"/>
            </fileset>
            <filterchain>
                <replacetokens>
                    <token key="PFW_VERSION" value="${version}"/>
                </replacetokens>
            </filterchain>
        </move>
        <jsMin targetDir="dist" suffix="" failOnError="false">
            <fileset dir="export">
                <include name="*.js"/>
            </fileset>
        </jsMin>
        <move todir="dist">
            <fileset dir="export">
                <exclude name="build.xml"/>
                <exclude name="composer.*"/>
                <exclude name="coverage.xml"/>
                <exclude name="phpcompatinfo.json"/>
                <exclude name="tests/**"/>
            </fileset>
        </move>
        <delete dir="export"/>
        <copy file="dist/config/config.php"
              tofile="dist/config/defaultconfig.php"/>
        <copy file="dist/languages/en.php" tofile="dist/languages/default.php"/>
        <zip destfile="Pfw_XH-${version}.zip" basedir="dist"
             prefix="pfw/"/>
        <delete dir="dist"/>
    </target>

</project>
